spring:
  application:
    name: uds
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:uds
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
